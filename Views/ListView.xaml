<Page x:Class="CryptoViewer.Views.ListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ListView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox"
                 Grid.Row="0"
                 Height="30"
                 Margin="10"
                 FontSize="16"
                 Padding="5"
                 TextChanged="SearchBox_TextChanged" 
                 Tag="Введіть назву монети..."/> 
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Coins}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  RowHeight="35"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Image" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImageUrl}" Width="25" Height="25" 
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" FontWeight="Bold"/>
                <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="80" />
                <DataGridTextColumn Header="Price" Binding="{Binding CurrentPrice, StringFormat={}{0:C}}" Width="120" />
                <DataGridTextColumn Header="24h Change" 
                                    Binding="{Binding PriceChangePercentage24H, StringFormat={}{0:P2}}" 
                                    Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>