<Page x:Class="CryptoViewer.Views.DetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="DetailView">

    <Grid Background="#222222">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <Image Source="{Binding ImageUrl}" Width="100" Height="100" Margin="0,0,0,20"/>

            <TextBlock Text="{Binding Name}" 
                       Foreground="White" 
                       FontSize="40" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding CurrentPrice, StringFormat={}{0:C}}" 
                       Foreground="LightGreen" 
                       FontSize="30" 
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>

            <Button Content="Назад до списку" 
                    Click="Button_Click"
                    Padding="20,10" Margin="0,50,0,0" FontSize="16"/>
        </StackPanel>
    </Grid>
</Page>